import { Reference } from "./types"

// Records should be added here to be indexed / made searchable
const references: Array<Reference> = [
  {
    collection: "elasticstore",
    index: "elasticstore",
  },
]

export default references
